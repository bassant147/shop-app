import React from 'react';
import { withRouter } from 'react-router-dom';

const ProductList = () => {

  return (
    <div>
      ProductList
    </div>
  );
}

export default withRouter(ProductList);